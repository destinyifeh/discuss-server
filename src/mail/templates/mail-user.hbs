<html>
  <head>
    <meta charset='UTF-8' />
    <title>New Message</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f9f9f9;
        padding: 20px;
        color: #333;
      }
      .container {
        max-width: 600px;
        margin: 0 auto;
        background: #fff;
        border-radius: 10px;
        padding: 24px;
        box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.05);
      }
      .header {
        font-size: 20px;
        font-weight: bold;
        margin-bottom: 15px;
        color: #222;
      }
      .message {
        background: #f4f4f4;
        padding: 15px;
        border-left: 4px solid #2563eb;
        border-radius: 6px;
        margin: 15px 0;
        font-size: 15px;
        line-height: 1.6;
      }
      .footer {
        margin-top: 25px;
        font-size: 13px;
        color: #777;
        text-align: center;
      }
      .sender {
        margin-top: 10px;
        font-size: 14px;
        color: #444;
      }
    </style>
  </head>
  <body>
    <div class='container'>
      <div class='header'>📩 New Message from {{senderName}}</div>

      <p>Hello {{recipientName}},</p>

      <p>You’ve received a new message via <strong>{{appName}}</strong>:</p>

      <div class='message'>
        {{messageContent}}
      </div>

      <p class='sender'>
        You can reply directly to
        <strong>{{senderName}}</strong>
        at:
        <a href='mailto:{{senderEmail}}'>{{senderEmail}}</a>
      </p>

      <div class='footer'>
        Sent via
        {{appName}}. Please do not reply to this system email.
      </div>
    </div>
  </body>
</html>